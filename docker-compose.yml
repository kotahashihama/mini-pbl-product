version: '3'
services:
  app:
    container_name: app
    image: rails
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./project:/app
    ports:
      - "3000:3000"
    tty: true
    stdin_open: true
    restart: always
